(ns org.datavec.api.transform.reduce.Reducer$ConditionalReduction
  (:refer-clojure :only [require comment defn ->])
  (:import [org.datavec.api.transform.reduce Reducer$ConditionalReduction]))

(defn ->conditional-reduction
  "Constructor."
  (^Reducer$ConditionalReduction []
    (new Reducer$ConditionalReduction )))

