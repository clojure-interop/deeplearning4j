(ns org.datavec.api.transform.split.RandomSplit
  "Created by Alex on 5/03/2016."
  (:refer-clojure :only [require comment defn ->])
  (:import [org.datavec.api.transform.split RandomSplit]))

(defn ->random-split
  "Constructor."
  (^RandomSplit []
    (new RandomSplit )))

