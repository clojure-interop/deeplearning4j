(ns org.datavec.api.split.partition.PartitionMetaData
  (:refer-clojure :only [require comment defn ->])
  (:import [org.datavec.api.split.partition PartitionMetaData]))

(defn ->partition-meta-data
  "Constructor."
  (^PartitionMetaData []
    (new PartitionMetaData )))

