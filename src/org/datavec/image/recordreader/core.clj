(ns org.datavec.image.recordreader.core
  (:refer-clojure :only [require comment defn ->])
  (:import ))

(require '[org.datavec.image.recordreader.BaseImageRecordReader])
(require '[org.datavec.image.recordreader.ImageRecordReader])
(require '[org.datavec.image.recordreader.VideoRecordReader])
