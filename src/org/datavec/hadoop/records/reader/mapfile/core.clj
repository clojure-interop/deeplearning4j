(ns org.datavec.hadoop.records.reader.mapfile.core
  (:refer-clojure :only [require comment defn ->])
  (:import ))

(require '[org.datavec.hadoop.records.reader.mapfile.IndexToKey])
(require '[org.datavec.hadoop.records.reader.mapfile.MapFileReader])
(require '[org.datavec.hadoop.records.reader.mapfile.MapFileRecordReader])
(require '[org.datavec.hadoop.records.reader.mapfile.MapFileSequenceRecordReader])
