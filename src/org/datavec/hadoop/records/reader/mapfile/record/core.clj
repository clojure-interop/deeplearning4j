(ns org.datavec.hadoop.records.reader.mapfile.record.core
  (:refer-clojure :only [require comment defn ->])
  (:import ))

(require '[org.datavec.hadoop.records.reader.mapfile.record.RecordWritable])
(require '[org.datavec.hadoop.records.reader.mapfile.record.SequenceRecordWritable])
