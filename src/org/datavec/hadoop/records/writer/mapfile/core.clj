(ns org.datavec.hadoop.records.writer.mapfile.core
  (:refer-clojure :only [require comment defn ->])
  (:import ))

(require '[org.datavec.hadoop.records.writer.mapfile.AbstractMapFileWriter])
(require '[org.datavec.hadoop.records.writer.mapfile.MapFileRecordWriter])
(require '[org.datavec.hadoop.records.writer.mapfile.MapFileSequenceRecordWriter])
