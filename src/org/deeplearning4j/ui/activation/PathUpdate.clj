(ns org.deeplearning4j.ui.activation.PathUpdate
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.ui.activation PathUpdate]))

(defn ->path-update
  "Constructor."
  (^PathUpdate []
    (new PathUpdate )))

