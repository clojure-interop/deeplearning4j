(ns org.deeplearning4j.spark.parameterserver.accumulation.SharedTrainingAccumulationTuple
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.spark.parameterserver.accumulation SharedTrainingAccumulationTuple]))

(defn ->shared-training-accumulation-tuple
  "Constructor."
  (^SharedTrainingAccumulationTuple []
    (new SharedTrainingAccumulationTuple )))

