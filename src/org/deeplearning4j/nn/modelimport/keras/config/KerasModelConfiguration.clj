(ns org.deeplearning4j.nn.modelimport.keras.config.KerasModelConfiguration
  "Basic properties and field names of serialised Keras models."
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.nn.modelimport.keras.config KerasModelConfiguration]))

(defn ->keras-model-configuration
  "Constructor."
  (^KerasModelConfiguration []
    (new KerasModelConfiguration )))

