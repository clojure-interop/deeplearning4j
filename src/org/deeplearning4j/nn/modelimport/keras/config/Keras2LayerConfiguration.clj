(ns org.deeplearning4j.nn.modelimport.keras.config.Keras2LayerConfiguration
  "All relevant property fields of keras 2.x layers."
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.nn.modelimport.keras.config Keras2LayerConfiguration]))

(defn ->keras-2-layer-configuration
  "Constructor."
  (^Keras2LayerConfiguration []
    (new Keras2LayerConfiguration )))

