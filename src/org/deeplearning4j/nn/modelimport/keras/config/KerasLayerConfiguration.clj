(ns org.deeplearning4j.nn.modelimport.keras.config.KerasLayerConfiguration
  "All relevant property fields of keras layers.

 Empty String fields mean Keras 1 and 2 implementations differ,
 supplied fields stand for shared properties."
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.nn.modelimport.keras.config KerasLayerConfiguration]))

(defn ->keras-layer-configuration
  "Constructor."
  (^KerasLayerConfiguration []
    (new KerasLayerConfiguration )))

