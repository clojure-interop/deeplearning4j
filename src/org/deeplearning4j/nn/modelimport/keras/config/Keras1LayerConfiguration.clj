(ns org.deeplearning4j.nn.modelimport.keras.config.Keras1LayerConfiguration
  "All relevant property fields of keras 1.x layers."
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.nn.modelimport.keras.config Keras1LayerConfiguration]))

(defn ->keras-1-layer-configuration
  "Constructor."
  (^Keras1LayerConfiguration []
    (new Keras1LayerConfiguration )))

