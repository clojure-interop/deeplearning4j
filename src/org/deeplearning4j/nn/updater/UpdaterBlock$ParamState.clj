(ns org.deeplearning4j.nn.updater.UpdaterBlock$ParamState
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.nn.updater UpdaterBlock$ParamState]))

(defn ->param-state
  "Constructor."
  (^UpdaterBlock$ParamState []
    (new UpdaterBlock$ParamState )))

