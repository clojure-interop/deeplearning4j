(ns org.deeplearning4j.aws.emr.EmrConfig
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.aws.emr EmrConfig]))

(defn ->emr-config
  "Constructor."
  (^EmrConfig []
    (new EmrConfig )))

