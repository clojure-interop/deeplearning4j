(ns org.deeplearning4j.optimize.listeners.SharedGradient
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.optimize.listeners SharedGradient]))

(defn ->shared-gradient
  "Constructor."
  (^SharedGradient []
    (new SharedGradient )))

