(ns org.deeplearning4j.optimize.listeners.checkpoint.core
  (:refer-clojure :only [require comment defn ->])
  (:import ))

(require '[org.deeplearning4j.optimize.listeners.checkpoint.Checkpoint])
(require '[org.deeplearning4j.optimize.listeners.checkpoint.CheckpointListener$Builder])
(require '[org.deeplearning4j.optimize.listeners.checkpoint.CheckpointListener])
