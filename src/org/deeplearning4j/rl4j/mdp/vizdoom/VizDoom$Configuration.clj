(ns org.deeplearning4j.rl4j.mdp.vizdoom.VizDoom$Configuration
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.rl4j.mdp.vizdoom VizDoom$Configuration]))

(defn ->configuration
  "Constructor."
  (^VizDoom$Configuration []
    (new VizDoom$Configuration )))

