(ns org.deeplearning4j.arbiter.ui.UpdateStatus
  (:refer-clojure :only [require comment defn ->])
  (:import [org.deeplearning4j.arbiter.ui UpdateStatus]))

(defn ->update-status
  "Constructor."
  (^UpdateStatus []
    (new UpdateStatus )))

