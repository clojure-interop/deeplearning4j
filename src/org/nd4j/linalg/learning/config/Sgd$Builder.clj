(ns org.nd4j.linalg.learning.config.Sgd$Builder
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.learning.config Sgd$Builder]))

(defn ->builder
  "Constructor."
  (^Sgd$Builder []
    (new Sgd$Builder )))

