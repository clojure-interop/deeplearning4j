(ns org.nd4j.linalg.learning.config.AMSGrad$Builder
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.learning.config AMSGrad$Builder]))

(defn ->builder
  "Constructor."
  (^AMSGrad$Builder []
    (new AMSGrad$Builder )))

