(ns org.nd4j.linalg.learning.config.Adam$Builder
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.learning.config Adam$Builder]))

(defn ->builder
  "Constructor."
  (^Adam$Builder []
    (new Adam$Builder )))

