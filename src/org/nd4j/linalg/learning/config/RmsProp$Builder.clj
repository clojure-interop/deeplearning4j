(ns org.nd4j.linalg.learning.config.RmsProp$Builder
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.learning.config RmsProp$Builder]))

(defn ->builder
  "Constructor."
  (^RmsProp$Builder []
    (new RmsProp$Builder )))

