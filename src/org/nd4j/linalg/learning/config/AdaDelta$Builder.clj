(ns org.nd4j.linalg.learning.config.AdaDelta$Builder
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.learning.config AdaDelta$Builder]))

(defn ->builder
  "Constructor."
  (^AdaDelta$Builder []
    (new AdaDelta$Builder )))

