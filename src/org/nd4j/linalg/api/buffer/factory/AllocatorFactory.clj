(ns org.nd4j.linalg.api.buffer.factory.AllocatorFactory
  "Created by agibsonccc on 2/25/16."
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.api.buffer.factory AllocatorFactory]))

(defn ->allocator-factory
  "Constructor."
  (^AllocatorFactory []
    (new AllocatorFactory )))

