(ns org.nd4j.linalg.api.blas.impl.SparseBaseLevel
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.api.blas.impl SparseBaseLevel]))

(defn ->sparse-base-level
  "Constructor."
  (^SparseBaseLevel []
    (new SparseBaseLevel )))

