(ns org.nd4j.linalg.api.blas.impl.BaseLevel
  "Provides auxillary methods for
 blas to databuffer interactions"
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.api.blas.impl BaseLevel]))

(defn ->base-level
  "Constructor."
  (^BaseLevel []
    (new BaseLevel )))

