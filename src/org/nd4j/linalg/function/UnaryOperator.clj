(ns org.nd4j.linalg.function.UnaryOperator
  "A specialization of Function where the input and return types are the same"
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.function UnaryOperator]))

