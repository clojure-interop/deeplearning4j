(ns org.nd4j.linalg.cpu.nativecpu.SparseBlasWrapper
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.cpu.nativecpu SparseBlasWrapper]))

(defn ->sparse-blas-wrapper
  "Constructor."
  (^SparseBlasWrapper []
    (new SparseBlasWrapper )))

