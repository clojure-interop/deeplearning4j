(ns org.nd4j.linalg.io.VfsUtils
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.linalg.io VfsUtils]))

(defn ->vfs-utils
  "Constructor."
  (^VfsUtils []
    (new VfsUtils )))

