(ns org.nd4j.imports.graphmapper.ImportState
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.imports.graphmapper ImportState]))

(defn ->import-state
  "Constructor."
  (^ImportState []
    (new ImportState )))

