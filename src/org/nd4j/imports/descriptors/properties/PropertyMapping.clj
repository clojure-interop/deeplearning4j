(ns org.nd4j.imports.descriptors.properties.PropertyMapping
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.imports.descriptors.properties PropertyMapping]))

(defn ->property-mapping
  "Constructor."
  (^PropertyMapping []
    (new PropertyMapping )))

