(ns org.nd4j.imports.descriptors.onnx.OnnxDescriptor
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.imports.descriptors.onnx OnnxDescriptor]))

(defn ->onnx-descriptor
  "Constructor."
  (^OnnxDescriptor []
    (new OnnxDescriptor )))

