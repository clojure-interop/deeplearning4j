(ns org.nd4j.imports.descriptors.onnx.OpDescriptor
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.imports.descriptors.onnx OpDescriptor]))

(defn ->op-descriptor
  "Constructor."
  (^OpDescriptor []
    (new OpDescriptor )))

