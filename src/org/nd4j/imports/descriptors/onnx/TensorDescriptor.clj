(ns org.nd4j.imports.descriptors.onnx.TensorDescriptor
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.imports.descriptors.onnx TensorDescriptor]))

(defn ->tensor-descriptor
  "Constructor."
  (^TensorDescriptor []
    (new TensorDescriptor )))

