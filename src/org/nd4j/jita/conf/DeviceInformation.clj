(ns org.nd4j.jita.conf.DeviceInformation
  (:refer-clojure :only [require comment defn ->])
  (:import [org.nd4j.jita.conf DeviceInformation]))

(defn ->device-information
  "Constructor."
  (^DeviceInformation []
    (new DeviceInformation )))

